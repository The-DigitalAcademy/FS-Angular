import { CarHighlightDirective } from './car-highlight.directive';

describe('CarHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CarHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
